version: '3'
services:
  cloudboard:
    image: cloudboard
    platform: linux/amd64
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "80:80"
    links:
      - postgres

  postgres:
    image: postgres:latest
    platform: linux/amd64
    ports:
      - "5432:5432"
